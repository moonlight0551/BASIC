1	REM THIS RUNS UNDER MBASIC ON A RC2014 RUNNING CP/M 2.2. IT COMES FROM A BOOK COMPUTER SPACEGAMES
2	REM YOU ARE TRYING TO BLOW UP ELRON THE INVISIBLE ALIEN IN A 10 X 10 GRID
3	REM YOUR ARE ASKED WHERE TO PUT THE BOMB. X CAN BE 0 TO 9, Y CAN BE 0 TO 9
4	REM AND D CAN BE 0 TO 9. 0, 0, 0, = BOTTOM LHS OF A GRID(SW)
5	REM TOP LHS IS NW AND BOTTOM RHS IS SE
10	PRINT "EVIL ALIEN"
20	LET S=10
30	LET G=4
35	RANDOMIZE
40	LET X=INT(RND(1)*S)
50	LET Y=INT(RND(1)*S)
60	LET D=INT(RND(0)*S)
70	FOR I=1 TO 6
80	PRINT "X POSITION (0 TO 9)?"
85	INPUT X1
90	PRINT "Y POSITION (0 TO 9)?"
100	INPUT Y1
110	PRINT "DISTANCE (0 TO 9)?"
120	INPUT D1
130	IF X=X1 AND Y=Y1 AND D=D1 THEN GOTO 300
140	PRINT "SHOT WAS ";
150	IF Y1>Y THEN PRINT "NORTH";
160	IF Y1<Y THEN PRINT "SOUTH";
170	IF X1>X THEN PRINT "EAST";
180	IF X1<X THEN PRINT "WEST";
190	PRINT
220	NEXT I
230	PRINT "YOUR TIME HAS RUN OUT ! ! "
240	STOP
300	PRINT "#BOOM# YOU GOT HIM !"
310	STOP
